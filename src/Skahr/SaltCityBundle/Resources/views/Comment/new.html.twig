{% extends 'SkahrSaltCityBundle::base.html.twig' %}
{% block body -%}
<div class="container">
	{% if app.session.get('login') %}
    	<h1>Создание отзыва</h1>
		<a href="{{ path('admin') }}">Назад</a>
    	{{ form(form) }}
	{% else %}
		<div class="alert alert-danger" role="alert">Доступ запрещен</div>
	{% endif %}
</div>
{% endblock %}
