
<div id="myCarousel" class="carousel slide" data-ride="carousel">
      
      <div class="carousel-inner" role="listbox">
          <div class="item active">
			<img class="first-slide" src="{{ asset('images/bckg/pic0_logo.jpg') }}">
            </div>
          {% set string = "123456789" %}
		  {% for entity in entities %}
            {% set i = random(string) %}
            {% set string = string|trim(i) %}
		  	<div class="item">
                <img class="first-slide" src="{{ asset('images/bckg/pic' ~ i ~ '_blur.jpg') }}">
                <div class="carousel-caption">
                  <h1>{{ entity.salestext }}</h1>
                  <p><a href="{{ path('sale') }}">все события</a></p>
                </div>
            </div>
		  {% endfor %}
		  
      </div>
      <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
      </a>
      <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
      </a>
    </div><!-- /.carousel -->
